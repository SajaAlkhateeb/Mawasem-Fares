services:
  - type: web
    name: mawasem-api
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "python manage.py migrate --noinput && gunicorn core.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com,mawsemjo.com,www.mawsemjo.com"
      - key: FRONTEND_URL
        value: "https://mawsemjo.com"
      - key: PYTHON_VERSION
        value: "3.13.7"
      - key: DATABASE_URL
        fromDatabase:
          name: mawasem-db
          property: connectionString
      - key: EMAIL_HOST
        value: "mail.mawsemjo.com"
      - key: EMAIL_PORT
        value: "465"
      - key: EMAIL_USE_SSL
        value: "true"
      - key: EMAIL_HOST_USER
        value: "noreply@mawsemjo.com"
      - key: DEFAULT_FROM_EMAIL
        value: "Mawasem <noreply@mawsemjo.com>"
      - key: EMAIL_HOST_PASSWORD
        sync: false

databases:
  - name: mawasem-db
    plan: free
